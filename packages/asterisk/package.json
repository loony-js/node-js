{
  "name": "asterisk",
  "version": "0.1.0",
  "main": "src/client.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "rm -rf dist && tsc && node ./dist/index.js",
    "client": "node ./src/client.js"
  },
  "devDependencies": {
    "@types/node": "^22.5.5"
  },
  "dependencies": {
    "jimp": "^1.6.0",
    "sharp": "^0.33.5"
  }
}
